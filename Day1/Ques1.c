// Write a program to input two numbers and display their sum.
#include <stdio.h>

int main() {
    int num1, num2;

    // Taking two numbers as input
    scanf("%d %d", &num1, &num2);

    // Displaying the sum
    printf("Sum = %d\n", num1 + num2);

    return 0;
}
